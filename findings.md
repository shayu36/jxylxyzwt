# Findings

## 初始发现

- 工作目录：`D:\Users\lenovo\Desktop\挑战杯num1`
- 主 TeX：`FRBMain.tex`
- 参考文献候选文件：`references.bib`、`library.bib`
- 参考 PDF：`第三十五届_冯如杯__主赛道.pdf`
- 未发现仓库级 `AGENTS.md` 或已有规划文件。

## TeX/PDF 结构

- `FRBMain.tex` 的正文从第 300 行的 `项目背景` 开始，参考文献从第 678 行开始；主文件没有 `\\bibliography` 调用，参考文献是内联文本。
- PDF 可用 Poppler 直接提取 UTF-8 文本，无需 OCR。临时提取文件：`D:\\RunIdea\\fengru_reference.txt`。
- PDF 物理页映射：第 1 页封面，第 2 页中文摘要，第 3–4 页英文摘要，第 5–6 页目录，第 7–25 页正文（打印页 1–19），第 26–30 页参考文献（[1]–[50]）。

## 摘要/正文核对

- 中文摘要：去除 PDF 换行和页眉页脚后，与 `FRBMain.tex` 第 245–251 行逐字一致。
- 英文摘要：语义内容一致；PDF 中有断行连字符（如 `neurodevelopmen-`）及排版引号，TeX 使用 `--` 和 ``...'' 语法；另有 `children's`（TeX）与 `children’s`（PDF）字符差异。
- 正文：章节和主要段落顺序一致；至少一处实质文本差异：PDF 正文页 15（物理页 21）写作“个性化设置，如图 8 展示，通过……”，而 TeX 第 593 行缺少“如图 8 展示”。
- 正文还存在可见标点差异，例如 PDF 正文页 2 的早期干预段使用 ASCII `,`/`(PCIT)`，而 TeX 第 324、326、328 行使用中文逗号/全角括号；PDF 正文页 6 的眼动段使用 `.`/`,`，而 TeX 第 378 行使用中文句号/逗号。故严格逐字符结论为“不完全一致”。
- 含公式的段落无法仅凭 `pdftotext` 做可靠逐字符判断（PDF 会把 `Σ`、上下标、积分/乘积等拆成独立文本块）；已单独核对其周围说明文字和公式编号顺序。

## 按正确格式同步差异

- 视觉复核 PDF 物理页 13（打印页 7）和物理页 21（打印页 15）：中文正文使用中文标点，破折号和公式周围的排版属于模板既有风格，不因 `pdftotext` 的 ASCII 化结果而批量改写。
- 已在正文补入 PDF 明确出现的“如图 8 展示”。
- 英文摘要中的 `children's` 已改为 UTF-8 Unicode `children’s`，与 PDF 的排版字符一致；PDF 的断行连字符不写入 TeX 源码，英文引号继续使用 LaTeX 标准 ``...'' 形式。

## 参考文献替换

- `FRBMain.tex` 参考文献区域已有 PDF 的 [1]–[50] 条目；已按 PDF 校准 [19] 的 `Granny’s`、[34] 的 `today’s` 和 [49] 的 ASCII 括号 `(人文社会科学版)`。
- 条目数量静态检查为 50，正文引用编号覆盖 [1]–[50] 且无超范围编号。
- 当前工作目录中未发现先前盘点时列出的 `references.bib`、`refsheet.tex`；未擅自重建或修改它们。

## 最终静态验证

- `FRBMain.tex` 基础大括号检查：平衡；所有 `\\begin{...}`/`\\end{...}` 环境计数一致（figure 11、table 1、equation 5）。
- `chktex` 已运行，退出码 2，输出为模板已有的格式/标点警告（如 dash 长度、命令后空格、图片引用空格、ASCII/中文标点混用），未执行编译。
- 本轮修改后重新运行静态核对：中文摘要归一化后逐字一致；英文摘要在处理 PDF 断行连字符、页码和 LaTeX 引号语法后语义文本一致；参考文献仍为 50 条且正文引用覆盖 [1]–[50]；括号/环境结构检查通过；`git diff --check` 通过。
- 本轮 `chktex` 仍以退出码 2 结束，警告集中在模板既有写法和参考文献 URL/连字符格式，未发现由本次差异修改引入的结构错误。
- `texcount` 能读取文件但报告 23 个解析错误，说明其对本模板/Unicode 的解析不可靠；不能作为编译正确性的证据。独立静态配对检查结果仍为 OK。
- 工作目录只有原参考 PDF；未生成 `FRBMain.pdf`，符合“不需要编译出 PDF”的要求。
